/* diferença de objeto em java script e JSON 
objeto em js ele pode ter metodos e na sua chave não usa "" exemplo:
let pessoa = {
    nome:'Miguel'
}->objeto js

Json ele não tem metodos só chave e valor e usa em sua chave "" exemplo:
let pessoa = {
   "nome":"Miguel"
}-> Json

*/


let pessoa = {
    "nome":"Ruty Daniely",
    "idade": 27,
    "religiao": "Evanlegica"
}

/*console.log(`${pessoa.nome} tem ${pessoa.idade} de idade, e é ${pessoa.religiao}.`);/*/

console.log(pessoa);
