// empt values: ou seja valores vazios
/*quando não atribuimos valores em uma variavel ou a variavel não tem
valor pode da um undefined ou null, caso se deparar com algum caso desses
que dizer que os valores não existem!*/

console.log(null);
console.log(undefined);

let a;// aqui não definimos valor na variavel como let a = 5;
console.log(a);